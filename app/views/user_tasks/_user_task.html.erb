<div class="well">
  <%= user_task.description %>
  <%= user_task.due %>
  <%= link_to fa_icon('pencil', text: 'Edit'), edit_user_task_path(user_task), remote: true %>
  <%= link_to fa_icon('trash', text: 'Delete'), user_task, remote: true, method: :delete, data: { confirm: 'Are you sure?' } %>
</div>